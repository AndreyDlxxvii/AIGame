using UnityEngine;

namespace AIGame
{
    public class WeeklyRewardView : DailyRewardView
    {
        private const string CurrentSlotInActiveKey = nameof(CurrentSlotInActiveKey);
        private const string TimeGetRewardKey = nameof(TimeGetRewardKey);
    }
}